VITE_API_BASE_URL=http://localhost:3000

# 'local' (default): x-local-role switcher, no real login (see backend's
# AUTH_MODE). 'cognito': real backend-mediated login — see auth/AuthGate.tsx.
VITE_AUTH_MODE=local
